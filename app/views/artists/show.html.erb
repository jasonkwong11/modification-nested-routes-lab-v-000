<h1><%= @artist.name %></h1>

<h1><%= link_to "New Song", new_artist_song_path(@artist) %></h1>
<ul>
SONGS:
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li>
  <% end %>
</ul>
